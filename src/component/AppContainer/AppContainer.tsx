import React from "react";

const AppContainer = ({ children }: any) => {
  return <div className="mx-auto" style={{paddingLeft: "10%", paddingRight: "10%"}}>{children}</div>;
};

export default AppContainer;
